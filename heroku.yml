setup:
  config:
    API_BASE_URL: API_BASE_URL
    DB_URL_CLUSTER: DB_URL_CLUSTER
    EMAIL_FROM: EMAIL_FROM
    EMAIL_PASSWORD: EMAIL_PASSWORD
    EMAIL_SERVICE: EMAIL_SERVICE
    EMAIL_USERNAME: EMAIL_USERNAME
    NODE_ENV: NODE_ENV
    PORT: PORT
    SECRET_KEY: SECRET_KEY
build:
  docker:
    web: Dockerfile